# v1.2.0 (2026/09/02)

## 更新概览

- **网站适配器框架（Site Adapters）**：针对不同网站定制不同的元数据提取、快照生成、阅读页面生成策略。
- **书签列表优化**：自动隐藏工具栏、快捷编辑全局快捷键、优化页面加载速度、优化前端细节
- **重构**：Favicon 架构、Huey 任务调度策略优化、Docker 镜像构建提速
- Bug 修复
- 依赖升级
- 合并上游更新


完整说明如下：

## 网站适配器框架（Site Adapters）

针对不同网站定制不同的元数据提取、快照生成、阅读页面生成策略。
可解决元数据/快照获取不到，元数据/阅读页面提取元素不正确，元数据、快照、阅读页面需清理等问题。

- **核心能力**:
    - 声明式适配：提供丰富且强大的[框架字段](./docs/reference/adapters-zh.jsonc)，在一个配置文件内完成全部网站适配
    - [自定义脚本](./docs/reference/adapters-scripts-hook.md)：支持在元数据、快照的 before/after 阶段执行，或完全替代元数据、快照的内置引擎
    - 认证体系：支持 Cookie、HTTP Header、OAuth2 Token、HTTP Basic Auth 四种认证方式
    - 测试面板：一个页面搞定全部配置的测试，提供全面详尽的调试信息
    - 订阅管理：网站适配器支持被发布为远程订阅源；管理员可通过界面实现对订阅源的新增、删除、更新、启用禁用、优先级排序等
- **参考资源**: 
    - [网站适配器指南](./docs/site-adapters.md)
    - [官方适配器订阅源](https://github.com/WooHooDai/linkding-cn-adapters)：已预先内置，可自由停用/删除。长期维护，适配常见网站。
- **场景示例**:
    - 元数据：元数据为JS加载，需浏览器环境; 使用正文第一张图作为预览图; 清理标题结尾的网站名后缀
    - 快照：PC端页面需登录方可访问正文（支持使用 Cookie 或 改写 URL 为移动端）; 轮播图在快照中无法完整查看; 清理 header/footer/ad 等无用元素
    - 阅读页面: 需包含评论区内容

> 旧的 自定义脚本 能力及实现方式已被废弃，不作兼容

## 书签列表优化

- **自动隐藏工具栏**：书签列表工具栏支持自动隐藏（可在通用设置中配置），桌面端鼠标悬浮显示，移动端点击空白区域显示
- **快捷编辑全局快捷键**：键盘按键`q`可触发鼠标下书签元素的内联编辑
- 优化页面加载速度：
    - 侧边栏懒加载：默认关闭时跳过服务端渲染，首次打开时 AJAX 加载，减少首屏开销
    - Favicon 懒加载：首页 favicon 图片懒加载，优化首页加载速度
    - "其他"域名节点按需加载：侧边栏域名"其他"分类下的子域名改为按需加载，减少首页 HTML 体积
- 前端优化
    - 侧边栏浮层模式：「显示侧边栏」关闭时，PC 端通过侧边栏按钮开启的侧边栏为浮层模式（overlay），避免页面布局重排
    - 统一 toast 通知系统
    - 侧边栏标签树模式样式优化：子节点增加竖线连接，层级关系更清晰；行级 hover 背景
    - 统一侧边栏计数样式，优化链接 hover 下划线行为
    - 预览图远程 URL 使用 `referrerpolicy="strict-origin-when-cross-origin"`，尽量避免因盗链无法显示远程图片

## 重构

- Favicon 架构
    - 全新多 provider 架构: 多源获取 + 健康探测 + 自建兜底解析，提高 favicon 成功率；支持手动放置 favicon 文件自动同步到数据库
    - 消除 DB / 磁盘不一致: Django view 替代 uwsgi static-map；移除 `Bookmark.favicon_file`，前端统一通过 `/favicon/{domain}` 获取
    - 定时刷新替代访问过期触发：服务器按计划主动更新，不再在用户访问时被动触发，避免 favicon 任务抢占元数据和快照任务线程
    - 资源优化
        - FaviconCache MISSING 状态渐进重试（1→7 天），永不放弃；过期刷新失败时保留旧图标
        - favicon 视图添加 ETag 缓存，减少重复请求
        - 优化获取策略，减少 favicon 体积：优先选择最接近 32×32 的候选，新增 ICO 单帧提取
        - 用户级检查：所有用户关闭"显示 Favicons"时跳过定时刷新任务，避免无人使用时浪费资源
        - 限制并发并防止重复入队，避免队列饥饿
        - 引入 `scheduling_priority` 字段，快照任务支持优先级调度，Favicon 任务不再阻塞元数据和快照任务
    - 文件写入原子性（临时文件 + rename），避免并发竞态
- Huey 任务调度策略
    - 统一优先级：阅读视图 > 手动快照 > 新书签 > 核心任务 > 订阅源 > favicon > 预览图
    - 调整HUEY worker: 总数调整为 4，非 P0 任务总并发限制为 3，预留 P0 阅读视图 worker
- Docker 镜像构建提速
    - 将无依赖的 pip install 和 npm ci 拆分为并行构建阶段，缩短关键路径耗时
    - 支持 `APT_MIRROR` 参数指定镜像源

## Bug 修复

- 书签列表
    - 修复 `title:` 搜索限定未回退到 url 字段
    - 修复标签输入框在拼音输入法下回车产生重复内容
    - 修复内联编辑描述后无法点击展开描述区域
    - 修复移动端快捷编辑标签时输入框被遮罩覆盖变灰
    - 修复书签重新抓取后预览图不更新
    - 修复书签详情查看页面重新抓取后，若已有描述不会被覆盖
    - 修复新增书签时已存在书签的预览图未显示
    - 修复导入书签时未对导入文件内部去重，导致重复 URL 导入失败
    - 修复 bookmarklet 在 Firefox 中无法工作
    - 修复侧边栏域名模块双层缓存导致视图模式与归一化失效
    - 修复侧边栏 — 侧边栏模块中错误包含高亮页面专属的颜色模块
    - 修复通用设置 — 侧边栏 — 侧边栏模块设置无法保存
    - 修复偏好设置切换后 `request.user_profile` 未同步更新
- 阅读模式
    - 修复阅读视图内容居中偏移及侧边栏布局问题
    - 修复阅读模式中超宽表格无法横向滚动
    - 修复 defuddle 对 CJK 内容的空格处理不当 
    - 修复 `reader-pending.js` 未加入前端构建入口
- Favicon
    - 修复 `<head>` 未显式声明 favicon 地址导致的 404
    - 修复 SVG 格式 favicon 被错误保存为 PNG
    - 修复误判 SVG 类型 favicon 为无效文件
    - 修复测试可能误删 favicon 文件
    - 修复 favicon 兜底图标 Cache-Control 改为 `no-cache` + ETag，确保后台任务完成后浏览器能立即拉取真实图标
    - 修复 Huey crontab 触发时间未遵循 `TIME_ZONE` 环境变量设置的时区
- 数据库：补充「smart_tree」选项变更的数据库迁移
- Docker 构建：修复 Dockerfile 遗漏 `COPY site_adapters/frontend` 和 `COPY site_adapters/styles`

## 依赖升级

- 升级 Django 6.0.7、bleach 6.4.0、playwright 1.62.0
- 从 DRF 主分支安装以修复 Django 6.0 的 `cc_delim_re` 兼容性
- 切换 docker 构建中的 single-file-cli 至官方最新版 2.1.3
- `simple-cdp` pin 至 1.8.6 以兼容 Node 20/22

## 上游更新合并

- 新增：RSS Feed 支持按用户名筛选
- 新增：支持按修改时间排序
- 新增：支持配置会话时长
- 新增：支持通过 API 删除标签
- 优化：标签 `#` 符号改用 CSS 渲染，可隐藏
- 优化：PWA maskable 图标增加内边距
- 优化：根视图重定向时保留查询字符串
- 修复：非默认 context path 下 Cookie 路径错误
- 修复：后台任务视图缺少认证
- 修复：HTML 导出中标签名未转义
- 修复：uBlock Origin 最新版下载可能失败

---

# v1.1.0 (2026/07/02)

## 核心更新

本次发版包含较多新功能或增强优化，核心概览如下：
- **阅读模式**：支持高亮批注，支持阅读设置，支持阅读位置记忆&恢复
- **高亮批注管理页面**：支持搜索、筛选、聚合、排序
- **书签列表增强**：(内容较多，请看下方完整说明)
- **侧边栏标签模块增强**：新增树状模式，快速探索标签关联
- **搜索语法优化**：限定语法简化为 `field:keyword`、`field:"key1 key2"`
- **标签自动补全增强**：支持中文全拼和拼音首字母前缀匹配；支持 Enter 保存并退出编辑
- **自动检查更新**

完整说明如下：

## 阅读模式

- 支持高亮批注：
    - 高亮颜色包括黄色、绿色、蓝色、粉色、主题色
    - 可在阅读页面侧边栏-高亮tab进行删除、修改高亮颜色、复制
    - 高亮tab工具栏显示模式支持在隐藏、常驻显示间切换 
    - 通用设置页面-高亮可定义复制按钮功能
        - 复制按钮功能：复制高亮&批注，仅复制高亮，仅复制批注
        - 自定义复制格式
    - 防丢失：正文若重新解析，高亮仍会进行重定位恢复；无法重定位恢复的高亮&批注会明确提示，不会自动删除
- 支持阅读设置：
    - 主题：跟随全局，浅色，深色
    - 字体：系统默认，无衬线，衬线，等宽
    - 字号
    - 页面宽度：自动根据视口宽度调整，也可手动调整
    - 行高
- 支持阅读时间预估：可自定义阅读速度使得预估符合个人情况
- 支持阅读位置记忆，每次进入阅读模式会询问是否恢复，可通过自然滚动覆盖记忆位置
- 可在阅读页面侧边栏-详情tab进行书签数据的编辑（能力等同于书签详情查看弹窗）
- 阅读页面正文提取引擎切换为 Defuddle
- 阅读页面正文提取支持向 Defuddle 传递参数进行自定义正文提取元素、屏蔽元素等，也支持使用自定义 Python 脚本替代 Defuddle 引擎
- 阅读页面正文提取优先基于 HTML 快照，因此无需针对同一个网站写两套支持脚本，仅需提供 HTML 快照的自定义脚本即可

## 高亮批注管理页面

- 搜索：支持直接搜索高亮、批注包含的文字，也支持限定关键词`hl`、`ann`
- 筛选：按高亮批注的批注（有/无）、颜色、日期；按高亮批注所在书签的域名、标签
- 聚合：无/书签/域名/颜色
- 排序：创建时间的升序/降序，修改时间的升序/降序，随机
- 编辑：高亮、批注支持原地编辑
- 复制：通用设置页-高亮可自定义复制按钮功能、复制格式、多条分隔符
- 批量操作：删除高亮，清楚批注，更改颜色，复制高亮，复制批注，复制全部

## 书签列表增强

- 书签快捷编辑
    - 书签工具栏新增 4 个按钮，可以一键切换书签的标题、描述、标签、笔记到编辑状态，原地进行编辑
    - 移动端编辑态会在文本框外区域增加遮罩，点击遮罩即可保存&退出
    - 移动端编辑态会自动将所编辑书签滚动到顶部，退出编辑态恢复原本位置，避免键盘遮挡
- 书签快捷标签：实现一键为书签新增/删除指定的单个或多个标签
    - 每个快捷标签支持显示模式在文字、图标之间切换
    - 每个快捷标签的图标支持自定义，支持搜索面板（iconify）
    - 每个快捷标签支持直接显示在书签工具栏，或收纳在一个统一的快捷标签目录按钮里
- 书签工具栏增强
    - 书签工具栏目前包括书签日期、书签动作、快捷编辑、快捷标签、书签状态 5 个模块，支持自定义模块顺序，支持启用/禁用指定模块
    - 每个模块内部的按钮都支持自定义排序、启用/禁用
    - 书签动作、快捷编辑、书签状态支持显示模式在文字、图标之间切换
    - 书签日期支持自定义跳转目标：禁用，最新快照，阅读模式，互联网存档，高亮与批注
    - 所有动作改为 API 调用 + UI 乐观更新，消除全页面刷新
- 随机按钮增强：
    - 支持启用/禁用随机按钮
    - 支持多种随机模式：列表，单条目
    - 若随机模式为单条目，支持多种目标：URL，阅读页面，HTML 快照，书签详情
    - 修复随机按钮筛选条件丢失问题
- 批量编辑增强：
    - 支持跨页勾选
    - 新增阅读模式相关动作：创建 HTML 文章，删除所有文章
- 书签详情查看弹窗优化：
    - 优化加载速度
    - 支持书签数据原地编辑
    - 书签状态样式改为 chip
- Favicon 重构
    - 全局架构由书签级别改为域名级别，大幅减少网络请求
    - 新增多 provider 支持 + 退火重试 + 自动修复机制，提高 favicon 成功率
    - 新增批量动作“刷新 Favicon”，支持手动更新指定书签 Favicon
    - 支持随自定义域名归一化同步
- 标题：移除标题长度限制，避免由此带来的各类问题；优化了 Bookmarklet 对此的兼容
- 描述：由悬浮 tooltip 改为点击展开/收缩完整内容
- 笔记：
    - 限制最大显示行数为15行
    - 显示态/编辑态无感切换，自动保持滚动位置
    - 显示/隐藏机制重构，全局笔记状态与单个书签笔记状态独立控制
- 预览图：
    - 预览图占位图背景透明，减少视觉阻塞感
    - 加载失败时也显示预览图占位图，避免显示图片破损样式
    - 支持开启预览图时，关闭预览图占位图
- 侧边栏：
    - 用户数据模块新增高亮批注相关数据
    - 移除 drawer/teleport 机制，确保侧边栏数据源单一
    - 滚动条空间始终预留，避免内容变化时视觉抖动

## 侧边栏标签模块增强

新增树状模式，将关联标签以嵌套树状形式展示，按包含书签数降序排列，快速发现标签模式

## 搜索语法优化

- 简化限定语法为 `field:keyword`、`field:"key1 key2"`，移除原限定语法`field:(keyword)`
- 新增限定词：`hl` 为高亮，`ann` 为批注

## 标签自动补全增强

- 拼音支持
    - 全拼前缀匹配：输入`shujufenxi`或部分可匹配到`数据分析`
    - 拼音首字母前缀匹配：输入`sjfx`或部分可匹配到`数据分析`
- Enter 保存：当自动补全下拉框未显示时，回车键会保存当前标签并退出编辑态

## 自动检查更新

- 若距离上次检查更新大于等于24小时，会自动检查更新
- 若发现更新版本
    - 会在顶部导航栏“设置”按钮处显示小圆点
    - 会在“设置”菜单栏中显示“新版本”，点击可打开新版本弹窗
- 新版本弹窗
    - 直接显示新版本的发布日志
    - 可跳转到新版本的发布页
    - 点击“知道了”，可消除“设置”按钮处的小圆点

## 其他

- 新增：HTML 快照失败自动重试机制
- 优化：“已分享”页面导航逻辑
    - 无论当前登录用户是否开启分享，都显示“已分享”入口
    - 若当前登录用户未开启分享，跳转到所有人的分享页面
    - 若当前登录用户开启分享，但暂无分享书签，跳转到所有人的分享页面
    - 若当前用户开启分享，且有分享书签，跳转到当前登录用户的分享页
- 优化：限制 Bundle 拖拽区域至图标，避免移动端误触
- 修复：URL 空参数残留，模板硬编码`?`
- 重构：SVG icon 风格统一为 Tabler Icon，抽取到 `svg_icons.html` 进行统一管理
- 重构：统一 localStorage key 命名

---
# v1.0.6（2026/05/17）

- 新增：侧边栏滚动位置记忆
- 新增：自定义域名归一化支持跨域名映射（详见 [Wiki](https://github.com/WooHooDai/linkding-cn/wiki/%E7%95%8C%E9%9D%A2%E6%93%8D%E4%BD%9C#1%EF%B8%8F%E2%83%A3-%E8%87%AA%E5%AE%9A%E4%B9%89%E5%9F%9F%E5%90%8D%E5%BD%92%E4%B8%80%E5%8C%96)）
- 新增：移动端下，支持拖动排序（Bundles 管理页；设置-通用-侧边栏功能模块）
- 新增：允许匿名用户调整侧边栏域名、标签的样式偏好

- 优化：分页器优化操作手感、显示更多页码
  - 翻页按钮固定在页面左右两端，可以连续点击下一页翻页
  - 翻页按钮由文字调整为 svg 图标，减少空间占用
  - 页码显示数量根据页面宽度动态调整，显示尽量多的页码
- 优化：移动端下，顶部工具栏（搜索框+按钮）始终独占一行，为搜索框提供更多可视空间
- 优化：批量编辑时：仅显示批量编辑工具条；增加“取消”按钮；统一工具条跟随、非跟随的样式
- 优化：侧边栏按钮由原本的文字“过滤器”调整为 svg 图标

- 修复：重构页面布局和粘性跟随实现，修复移动端 webview 的显示问题
- 修复：移动端下，集成设置页无法看到 API Token 的删除按钮
- 修复：快照中文乱码
- 修复：书签小程序【在浏览器端检测网站标题和描述】生成错误
- 修复：环境配置文件([.env.sample](./.env.sample))加入字符编码相关变量，避免上传文件为中文文件名引发服务器错误

---
# v1.0.5（2026/05/07）

- 修复：数据库迁移因去重未处理“url_normalized 为空”的情况，导致迁移不完整，
- 修复：侧边栏收纳状态下（侧边栏隐藏/宽度不足时），其功能模块的设置菜单无法弹出
- 修复：登录页面的语言切换菜单，在移动端下会被遮挡
- 修复：Dockerfile 中 uBlockOrigin 最新版获取方式导致的构建失败

---
# v1.0.4（2026/05/04）

> [!WARNING]
> 本版本存在数据库迁移BUG，v1.0.5 进行了修复（建议直接升级至 v1.0.5）

## 当前项目

- 新增：i18n 国际化改造，目前支持简体中文、English，可[新增支持语言](https://github.com/WooHooDai/linkding-cn/blob/main/docs/i18n-guide.md)
- 新增：侧边栏 用户数据 功能模块，支持热力图/日历图双模式，支持日期筛选联动，支持总结、星期显示/隐藏
    - <details>
      <summary>热力图模式</summary>
      <img width="300" height="372" alt="image" src="https://github.com/user-attachments/assets/dcfa1f3d-090e-4341-9c78-5bb9b574daf0" />
    </details>

    - <details>
      <summary>日历图模式</summary>
      <img width="300" height="393" alt="image" src="https://github.com/user-attachments/assets/76b6372f-a6d1-4c2b-a01e-33d9577aacda" />
    </details>

    - <details>
      <summary>显示星期 & 隐藏总结</summary>
      <img width="300" height="306" alt="image" src="https://github.com/user-attachments/assets/1242e325-eeb4-4aa2-9b20-1ae1f4ce45ea" />
    </details>
- 新增：侧边栏 域名 功能模块，支持图标/完整双模式支持自定义域名归一化
    - <details>
      <summary>图标模式</summary>
      <img width="300" height="314" alt="image" src="https://github.com/user-attachments/assets/e285fe81-7116-48df-b609-3723d3d0d6a3" />
    </details>

    - <details>
      <summary>完整模式</summary>
      <img width="300" height="530" alt="image" src="https://github.com/user-attachments/assets/ec03a8aa-10f2-4a14-b378-cf8ea5dc8a08" />
    </details>
- 新增：侧边栏功能模块支持独立启用/关闭，支持排序，可在通用设置中配置。
- 新增：过滤器（Bundles）支持如下新的筛选项：HTML 快照、预览图、Favicon
- 新增：批量操作支持如下新的动作：创建 HTML 快照、删除所有快照、移至回收站
- 新增：元数据异步补全与重试机制
- 新增：快照、元数据抓取支持为指定 URL 加载自定义配置文件

- 优化：重构通用设置页面、集成设置页面布局与交互：卡片式分区，分区导航栏，一键生效
    - <details>
      <summary>通用设置</summary>
      <img width="1000" height="2525" alt="image" src="https://github.com/user-attachments/assets/fb44ecaf-6721-4426-9075-678bc1cd9a9d" />
    </details>

    - <details>
      <summary>集成设置</summary>
      <img width="1000" height="1027" alt="image" src="https://github.com/user-attachments/assets/16e94917-ec46-45ed-a8a8-d660379cc633" />
    </details>
- 优化：重构批量操作动作分组与排序
- 优化：二次确认修改为弹窗模式
- 优化：URL 字段中第一个 http/https 链接会被字段抽取作为书签链接
- 优化：favicon 加载策略，优先复用全局缓存，后台异步刷新
- 优化：快照调整为即时调度，新增书签的快照更快开始获取
- 优化：新增/编辑书签页面 URL 字段增加防抖、竞态保护
- 优化：重构、调整、补充单元测试 & E2E测试

- 修复：并发导致的重复 URL 书签
- 修复：书签下的标签点击后会被重复添加到搜索条件

- 其他：升级各项依赖至最新，大版本升级结果如下：
  - django 6.0, huey 3.0, mozilla-django-oidc 5.0
  - @rollup/plugin-node-resify 16, @rollup/plugin-terser 1.0,postcss-nesting 14
  - node 22

## 合并上游原项目提交

- 新增：搜索引擎支持逻辑表达式（and, or, not）
- 新增：集成设置页面 API token 管理
- 新增：bookmarklet 元数据获取方案，支持服务器端优先、浏览器端优先
- 新增：支持禁用登录；支持 OIDC 登录；

- 优化：对视频与 PDF 资产的处理，收紧资源查看页 CSP
- 优化：重构前端为 component-only
- 优化：标签管理页面功能交互形式由独立页面改为弹窗
- 优化：标签聚合关闭时首字母不大写、不加粗
- 优化：切换到 uv 管理
- 优化：切换 Ruff lint/format

- 修复：Postgres 导入时缺少标签会导致错误
- 修复：导入书签时未进行 URL 规范化
- 修复：规范化 URL 缺失时的精确 URL 查重

---
# v1.0.3（2025/10/03）
## 合并上游原项目提交

- 新增：标签管理功能
- 新增：从当前搜索&筛选项新建过滤器
- 新增：批量创建 HTML 快照
- 新增：书签默认为分享的选项
- 新增：允许使用过滤器筛选 feeds
- 优化：支持 Ctrl/Cmd + Enter 提交书签
- 优化：URL 查重前归一化
- 优化：导入书签时，忽略超出长度限制的标签

## 当前项目

- 优化：预览图增强，解决 CORP 限制，支持分块传输图片
- 优化：favicon 增强，新增书签时预读取/预下载，新增后第一时间展现
- 优化：自定义脚本更改 URL 时，会二次查重
- 优化：批量编辑工具条粘性吸顶
- 优化：未读、分享的取消样式微调，更便于快速操作
- 优化：部分界面样式

---
# v1.0.2 (2025/09/18)

- 新增：自定义配置文件支持域名别名（或称配置共享）
- 修复：阅读模式无法滚动
- 修复：部分中文网页快照乱码
- 优化：滚动条紧贴右侧，且撑满整个窗口高度
- 优化：singlefile参数采取合并，优先级为：自定义配置文件 > 环境配置文件 > 内置参数

---
# v1.0.1 (2025/09/13)

- 增强&修复元数据、快照自定义获取能力（Wiki已更新说明）
    - 新增：自定义配置文件支持相对路径
    - 优化：singlefile服务参数调整
    - 优化：自定义配置singlefile参数强制使用词典形式
    - 修复：自定义脚本cookie解析错误
- 修复：移动端左右无边距

---
# v1.0.0 (2025/09/08)

第一版发布，相对linkding进行了如下修改和增强：

- 🇨🇳 中文本地化：界面翻译；标签聚合支持中文；时区与日期处理；favicon服务默认使用query.domains，无需🪜
- 🔍 搜索增强：新增支持限定搜索范围（标题、描述、笔记、url）
- 📅 筛选增强：新增日期筛选（支持绝对日期和相对日期）；新增标签状态筛选（有标签/无标签）
- 🎲 排序增强：新增随机排序；支持按删除时间排序（回收站中）
- 🤖 过滤器（Bundle）增强：新增支持二级过滤器；新增支持所有筛选项
- 🐞 元数据获取增强：默认解析支持更多主流网站；新增支持自定义脚本
- ⚡️ 快照增强：新增支持自定义脚本（引入drissionpage依赖）；新增支持重命名
- 📖 阅读模式增强：支持无快照书签开启阅读模式
- ♻️ 回收站：新增功能，书签删除时默认移动到回收站，可操作还原、永久删除，支持批量操作
- 👀 界面增强
    - 书签列表和过滤器显示书签数量
    - 粘性工具：搜索栏、分页导航栏、侧边栏可固定在屏幕上，方便操作
    - 快速筛选同域名标签：点击favicon快速筛选（支持自定义域名归一化规则）
    - 独立滚动条：书签列表、侧边栏滚动条互不影响
    - 位置记忆：编辑书签后书签列表浏览位置不变
    - 折叠状态记忆：同一会话中、侧边栏的过滤器、标签、二级过滤器折叠状态将被保持
    - 快速加载：预览图和favicon加载更快、更及时
- 🔧 样式增强：各类影响操作或美观的样式问题

---